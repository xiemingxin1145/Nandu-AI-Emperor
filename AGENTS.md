# 南渡无悔 · AI/开发者接手必读

本文件是仓库级执行入口。任何 AI、开发者、自动化代理在修改本项目之前，必须先阅读：

1. `docs/PROJECT_MASTER_PLAN.md` —— 项目总纲、当前里程碑、任务编号、下一步与完成记录。
2. `docs/RELEASE_POLICY.md` —— Android 版本号、签名、构建与交付硬规则。
3. 与当前任务直接相关的专项文档，例如 `docs/audio/`、历史 Canon、世界 AI、QA 文档。

## 一、接手流程

每次开始工作时，严格按下面顺序：

1. 打开 `docs/PROJECT_MASTER_PLAN.md`。
2. 确认 `当前版本`、`当前里程碑`、`当前下一任务`。
3. 优先处理当前里程碑中状态为 `NEXT` 的第一项；除非用户明确改变优先级，不得跳到后续里程碑。
4. 开工前把该任务状态改为 `IN_PROGRESS`，并写明分支/负责人（可写 AI 名称或“当前会话”）。
5. 实现代码、测试、构建与真机/静态验收。
6. 完成后把任务改为 `DONE`，补充：完成日期、提交/PR、验证结果、遗留问题。
7. 将下一项任务改为 `NEXT`，同时更新文档顶部的 `当前下一任务` 和里程碑完成数。
8. 如果任务被阻塞，标记 `BLOCKED`，写清阻塞原因和解除条件，不得假装完成。

## 二、状态定义

- `NEXT`：当前应该优先做的唯一任务。
- `TODO`：已规划，尚未开始。
- `IN_PROGRESS`：正在实施。
- `BLOCKED`：被明确条件阻塞。
- `DONE`：代码、测试、构建和验收均达到完成标准。
- `DEFERRED`：主动延期，必须写原因和重新评估节点。

同一里程碑原则上只能有一个 `NEXT`。可以有少量并行 `IN_PROGRESS`，但必须互不冲突。

## 三、项目不可违反的设计原则

### 1. 历史不是铁轨

历史事件是“锚点 + 条件 + 世界状态”的结果，不是按年份强制播放的剧本。

禁止：
- 到 1140 年无条件弹出“顺昌之战”；
- 在 UI 里硬编码岳飞、韩世忠、刘锜等人的后期官职；
- 无视人物生死、所在地、军队、势力、战争结果强行让历史人物出现；
- 玩家已经改变世界后仍强行播放原历史结局。

允许：
- 历史条件满足时产生近似历史事件；
- 条件被玩家改变时事件延迟、变形、替代或永不发生；
- AI 势力基于当前世界状态产生新的架空历史。

### 2. 世界状态是唯一真相源

人物官职、所在地、军团、存亡、派系、关系；城市归属、兵力、粮道；战争、外交、财政等，都必须从 `GameState` / 对应系统读取。

UI 只能展示状态，不能自己“编一个历史状态”。临时演示数据不得进入正式流程。

### 3. 玩家选择必须真正改变世界

任何“圣旨”“战役选择”“朝议裁断”“任免”“外交决定”都必须有真实状态变更或明确的延迟后果，不能只改页面局部变量或显示一句反馈文字。

### 4. 页面不能只有壳

正式入口必须满足至少一项：
- 有真实数据；
- 有可执行操作；
- 有动态事件；
- 有明确的“当前无事”状态与原因。

禁止出现“待办 1 件但进去空白”“按钮能点但什么都没发生”的页面。

### 5. AI 负责思考，本地规则负责裁决

AI 可以生成建议、计划、奏疏、外交意图、候选行动；不能直接绕过本地规则修改 `GameState`。所有世界改变必须经过本地校验和统一执行入口。

### 6. 版本和签名永远按仓库规则走

任何安装包任务，先读 `docs/RELEASE_POLICY.md`。不得复用旧 `versionCode`，不得临时更换测试签名。

## 四、每个任务的完成标准（Definition of Done）

一个任务只有同时满足以下条件才可标记 `DONE`：

- 核心逻辑已实现；
- 无明显硬编码绕过状态系统；
- 必要单元测试/回归测试已补；
- Android CI 构建通过；
- 涉及资源时已确认资源真实进入 APK；
- 涉及 UI 时至少做静态/真机验收；
- 涉及历史时已验证“历史发生”和“历史被改变后不发生/变形”两条路径；
- `docs/PROJECT_MASTER_PLAN.md` 已同步更新。

## 五、交接格式

任何 AI 在结束工作前，至少留下以下信息：

- 完成任务 ID：
- 修改分支：
- 关键提交/PR：
- CI/测试结果：
- APK/资源结果（如有）：
- 仍存在的问题：
- 下一任务 ID：

如果没有更新总纲和下一任务，视为交接未完成。
